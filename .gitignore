# No versionar tu entorno virtual
labeling-env/

# Entornos virtuales / caché de Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Pesos de modelos y artefactos pesados (no versionar; regenerar con train_corcho.py)
*.pt
*.onnx
yolo11n.pt

# Modelos y datasets POR UBICACIÓN (gitignored; se rastrean roi.yaml/NOTES.md/.gitkeep/README.md)
locations/*/detector.onnx
locations/*/detector.pt
locations/*/dataset/
models/general/*.onnx

# Salidas de entrenamiento de Ultralytics
runs/

# Salidas de detección por lotes (detect.py)
detections/

# Logs de ejecución
logs/

# Sesiones grabadas (frames/audio/eventos)
sessions/

# Dataset de zona extraído (frames/labels/montaje pesados; local para etiquetar)
dataset_zona/

# Capturas dedicadas de la mordida (frames/manifests/análisis pesados)
captures_bite/

# Cachés de dataset que regenera Ultralytics
dataset/labels/*.cache
*.cache